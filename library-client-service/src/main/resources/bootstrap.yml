spring:
  application:
    name: library-client-service
  cloud:
    config:
      uri: http://localhost:8888

server:
  port: 8086

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics


